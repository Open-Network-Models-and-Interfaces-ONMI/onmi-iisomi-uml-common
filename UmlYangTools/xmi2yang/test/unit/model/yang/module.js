const _module = require("../../../../model/yang/module");
const assert = require('chai').assert;
describe('model/yang/module', function() {
    describe('#new Model()', function () {
        it('stub result will return true', function () {
            assert.equal(true, true);
        });
    });
});