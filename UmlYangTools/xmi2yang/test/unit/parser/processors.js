const processors = require("../../../parser/processors");
const assert = require('assert');
describe('parser/processors', function() {
    describe('clazz:', function () {
        describe('#basicTypes()', function () {
            it('stub result will return true',function() {
                assert.equal(true, true);
            });
        });
        describe('#nodeUpdate()', function () {
            it('stub result will return true',function() {
                assert.equal(true, true);
            });
        });
        describe('#generalization()', function () {
            it('stub result will return true',function() {
                assert.equal(true, true);
            });
        });
        describe('#ownedAttribute()', function () {
            it('stub result will return true',function() {
                assert.equal(true, true);
            });
        });
        describe('#ownedParameter()', function () {
            it('stub result will return true',function() {
                assert.equal(true, true);
            });
        });
        describe('node:', function () {
            describe('#enum()', function () {
                it('stub result will return true',function() {
                    assert.equal(true, true);
                });
            });
        });

    });
});